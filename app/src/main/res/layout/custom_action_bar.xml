<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/customToolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/nav_white"
    android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Notifications button (left) -->

        <!-- Small image in middle -->

        <ImageButton
            android:id="@+id/btnNotifications"
            android:layout_width="54dp"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentBottom="true"
            android:layout_marginStart="-12dp"
            android:layout_marginEnd="76dp"
            android:layout_marginBottom="8dp"
            android:layout_toStartOf="@+id/centerLogo"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Notifications"
            android:padding="8dp"
            android:src="@drawable/notification" />

        <ImageView
            android:id="@+id/centerLogo"
            android:layout_width="161dp"
            android:layout_height="44dp"
            android:layout_centerInParent="true"
            android:contentDescription="App Logo"
            android:src="@drawable/navbar_banner" />

        <!-- Search button (right) -->
        <ImageButton
            android:id="@+id/btnSearch"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/outline_map_search_24"
            android:contentDescription="Search"
            android:padding="8dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"/>

    </RelativeLayout>
</androidx.appcompat.widget.Toolbar>
