<!-- res/drawable/ripple_button.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#33221800">  <!-- subtle ripple (semi-transparent) -->

    <!-- Visible content (always shown): solid gold pill -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/org_gold"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>

    <!-- Optional: mask to confine ripple to pill shape -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>
</ripple>
